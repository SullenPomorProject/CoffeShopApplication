<Page x:Class="WpfApp1.WorkPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WorkPage">

    <Grid>
        <TabControl Height="Auto">
            <TabItem Header="Профиль">
                <StackPanel HorizontalAlignment="Center">
                    <Label>Shop Address</Label>
                    <Label x:Name="ShopAddressLabel"></Label>
                </StackPanel>
            </TabItem>
            <TabItem Header="Заказы">
                <StackPanel>
                    <Button Click="UpdateButton_Click">Update</Button>
                    <Label>New Orders</Label>
                    <DataGrid x:Name="newOrdersDataGrid"/>
                    <Label>In Proccess</Label>
                    <DataGrid x:Name="processOrdersDataGrid"/>
                    <Label>Complited</Label>
                    <DataGrid x:Name="complitedOrdersDataGrid"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="Склад">
                <DataGrid d:ItemsSource="{d:SampleData ItemCount=5}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
